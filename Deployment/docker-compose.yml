version: '3'
services:
  react_client:
    container_name: kappindu-react
    restart: always
    image: kappacr.azurecr.io/soen390_19_react_client:s3
    ports:
      - '80:3000'
    networks:
     - kapp-app
  
  express_server:
    container_name: kappindu-express
    restart: always
    image: kappacr.azurecr.io/soen390_19_express_server:s3
    ports:
      - '5000:5000'
    networks:
     - kapp-app
    depends_on:
      - mongo    
  mongo:
    container_name: kappindu-mongo
    image: kappacr.azurecr.io/mongo:39019
    ports:
      - '27017:27017'
    networks:
      - kapp-app
    volumes:
      - mongo-data:/data/db

networks:
  kapp-app:
    driver: bridge
volumes:
  mongo-data:
    driver: local
